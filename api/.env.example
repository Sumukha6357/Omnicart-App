# --- BACKEND DOMAIN MASTER ---
API_PORT=8080
SPRING_PROFILES_ACTIVE=local

# Database (Local access)
DB_HOST=localhost
DB_PORT=5432
POSTGRES_DB=Omnicart_DB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
SPRING_DATASOURCE_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}

# Redis
SPRING_REDIS_HOST=localhost
SPRING_REDIS_PORT=6379

# Security
JWT_SECRET=titanium_omnicart_secure_key_at_least_32_chars
JWT_EXPIRATION=86400000

# Mail
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=465
SPRING_MAIL_USERNAME=kysumukha@gmail.com
SPRING_MAIL_PASSWORD=lkjwpqebxhytkwhc
